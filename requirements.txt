# -----------------------------------------------------------------------------
# Compass Project Dependencies
# -----------------------------------------------------------------------------
# Use this file to install all dependencies: pip install -r requirements.txt
# -----------------------------------------------------------------------------

# --- Core Application Framework ---
typer[all]>=0.9.0      # For the Command-Line Interface (CLI)
pyyaml>=6.0            # For parsing the config.yaml file
pydantic>=2.5.0        # For robust data validation and settings management

# --- AI / LLM Providers ---
openai>=1.12.0         # For OpenAI models (GPT-4o, etc.)
anthropic>=0.21.0      # For Anthropic models (Claude 3)
google-generativeai>=0.4.0 # For Google models (Gemini)

# --- General Connectors & HTTP Requests ---
requests>=2.28.0       # The standard for making HTTP API calls for most connectors

# --- Specific Connector Libraries ---
# You can install these as you build the connectors, but listing them here
# provides a complete picture of the project's ecosystem.

# Cloud / Infrastructure
boto3>=1.28.0          # The official AWS SDK for Python
azure-identity>=1.15.0 # For authenticating with Azure services
google-api-python-client>=2.90.0 # For various Google Cloud Platform APIs
kubernetes>=28.1.0     # For interacting with the Kubernetes API

# Note: Other connectors for platforms like PagerDuty, GitLab, Jira, etc.,
# will use the `requests` library and do not require their own specific SDKs.